{
  "name": "familyfin-couple-money-fit",
  "version": "1.0.0",
  "private": true,
  "description": "好理家在情侶金錢默契測驗公開 MVP",
  "scripts": {
    "check": "node --check data.js && node --check pairing.js && node --check scoring.js && node --check tracking.js && node --check app.js",
    "test": "node --test tests/*.test.js"
  }
}
